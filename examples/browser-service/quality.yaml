schema_version: 1
repository: browser-service
owner: storefront-team
checks:
  - id: browser
    kind: test
    argv: ["node", "scripts/run-playwright.mjs", "{python}", "{report}"]
    timeout_seconds: 120
    stages: [pull-request, merge, nightly]

