schema_version: 1
repository: dashboard-browser
owner: platform-team
checks:
  - id: dashboard-browser
    kind: test
    argv: ["node", "run.mjs", "{python}", "{report}"]
    timeout_seconds: 120
    stages: [pull-request, merge, nightly]
